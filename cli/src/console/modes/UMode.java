package console.modes;

import console.Command;
import console.contract.InputMode;

public class UMode implements InputMode {

    @Override
    public Command parseCommand(String input) {
        return null;
    }
}
